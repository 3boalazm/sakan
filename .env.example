# Firebase service account (paste the JSON or base64 of it)
FIREBASE_SERVICE_ACCOUNT=your_firebase_service_account_json_here

# Set to '1' to use fake in-memory Firestore (for local dev without Firebase)
# SAKAN_FAKE_FS=1
